1. 外部链接跳转
	路径：/tolink
	参数：redirecturl 必须为http链接地址 非空

	例子：
	/tolink?redirecturl=https://www.baidu.com/

2. 第三方通过token登录
	路径：/loginToken
	参数：siteid 企业ID  非空
		  token 登录验证token 非空
	
	例子：
	/loginToken?siteid=?&token=?

3. 主菜单跳转
   路径：/${route}
   route为主菜单配置入口的name值
   注：
   1. 当主菜单入口action配置为链接地址时，需要加参数route=${name},即为/friends?route=crm/ticket/robot
   2. 当route未匹配到对应路径，则会自动跳转到主菜单配置默认项
   
   例子：
	/setting
	/chats?siteid?&ntid=?&cid=?&tid=?
	/friends?route=crm/ticket/robot
	/kpi
	/phone
	/help

4. 会话跳转
	路径：/chats
	参数：siteid 企业ID  非空
		  ntid 访客ID
		  cid 会话ID
		  tid 会话模版
	例子：
	/chats?siteid=${SITEID}&ntid=${NTID}&cid=${CID}&tid=${TID}